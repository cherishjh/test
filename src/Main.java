import java.util.Arrays;

import static java.lang.Math.max;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {

        /*int numbers[]={1,2,3,4,5};
        int[] answer = new int[numbers.length];
        for (int i = 0; i < numbers.length; i++) {
            answer[i] = numbers[i] * 2;
        }
        System.out.println(Arrays.toString(answer));*/
//        int[] numbers = {2, 1, 3, 4, 1};
//        int n = 0;
//        int[] result = new int[numbers.length * (numbers.length - 1) / 2];
//        for (int i = 0; i < numbers.length; i++) {
//            for (int j = i + 1; j < numbers.length; j++) {
//                result[n] = numbers[i] + numbers[j];
//                n++;
//            }
//        }
//        Arrays.sort(result);
//        int k=0;
//        int [] result_1= new int [result.length];
//        for(int a=0; a<result.length-1; a++){
//            if(result[a]!=result[a+1]){
//                result_1[k]=result[a];
//                k++;
//            }
//            result_1[result.length-1]=result[result.length-1];
//        }
//        int[] answer = Arrays.copyOfRange(result_1, 0,k+1);
//        System.out.println(Arrays.toString(answer));
    }
}